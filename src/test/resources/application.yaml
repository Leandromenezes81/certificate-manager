spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database:
      platform: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false

management:
  metrics:
    export:
      newrelic:
        enabled: false

keycloak:
  enabled: false